# 轮足机器人下位机接口文档

本文档描述了轮足机器人下位机（STM32F407）与上位机（Jetson Nano/PC）之间的通信接口协议，供上位机开发人员使用。

---

## 1. 系统概述

### 1.1 硬件架构
- **MCU**: STM32F407VET6 (主频 168MHz)
- **操作系统**: FreeRTOS
- **通信接口**:
  - **USB CDC**: 虚拟串口，用于上位机通信
  - **UART2**: 舵机总线 1 (ID 1-6)，波特率 1Mbps
  - **UART4**: 舵机总线 2 (ID 7-18)，波特率 1Mbps
  - **UART3**: IMU 传感器 (JY901S)，DMA 接收
  - **UART5**: 4 路电机驱动板，DMA 收发

### 1.2 FreeRTOS 任务架构
| 任务名 | 优先级 | 功能描述 |
|--------|--------|----------|
| ServoTask | Realtime (最高) | 处理舵机控制命令，发送同步写指令 |
| FeedbackTask | High | 周期性读取舵机反馈 + IMU 数据，打包上传 |
| CommRxTask | High | 解析 USB 接收的指令包 |
| CommTxTask | High | 通过 USB 发送反馈数据包 |

---

## 2. USB 通信协议（上位机 ↔ 下位机）

### 2.1 物理层
- **接口**: USB Type-C / Micro-USB (STM32 USB 从机)
- **设备类型**: USB CDC (虚拟串口)
- **波特率设置**: 任意（推荐 115200 或 921600）

### 2.2 数据链路层（包格式）

所有数据包采用**小端字节序 (Little-Endian)**，结构如下：

```
+--------+--------+--------+--------+--------+--------+
| 0xA5   | 0x5A   | Type   | Len    | Data[] | ChkSum |
+--------+--------+--------+--------+--------+--------+
  1 byte  1 byte  1 byte  1 byte   N bytes   1 byte
```

| 字段 | 长度 | 说明 |
|------|------|------|
| 帧头 1 | 1 | `0xA5` |
| 帧头 2 | 1 | `0x5A` |
| 包类型 (Type) | 1 | 见下方包类型定义 |
| 数据长度 (Len) | 1 | Data 字段的字节数 |
| 数据 (Data) | 0-255 | 载荷数据 |
| 校验和 (ChkSum) | 1 | 前面所有字节累加和的低 8 位 |

**校验算法 (Python)**:
```python
checksum = sum(packet[:-1]) & 0xFF
```

### 2.3 包类型定义

| Type | 名称 | 方向 | 说明 |
|------|------|------|------|
| `0x01` | TYPE_PING | 双向 | 心跳/连接检测 |
| `0x10` | TYPE_SERVO_CTRL | 上位机→下位机 | 舵机位置控制 |
| `0x11` | TYPE_TORQUE_CTRL | 上位机→下位机 | 舵机扭矩使能控制 |
| `0x30` | TYPE_SENSOR_IMU | 下位机→上位机 | IMU 传感器数据 |
| `0x40` | TYPE_RL_STATE | 下位机→上位机 | 强化学习全状态包（IMU+ 舵机反馈） |
| `0xE0` | TYPE_SYS_ERROR | 下位机→上位机 | 系统错误信息 |

---

## 3. 控制指令（上位机 → 下位机）

### 3.1 舵机位置控制 (TYPE_SERVO_CTRL = 0x10)

**用途**: 控制最多 18 个舵机的目标位置、速度、加速度。

**Data 载荷结构**:
```
+-------+------------------+
| Count | ServoParams[]    |
+-------+------------------+
 1 byte   N * 8 bytes
```

**ServoCtrlParam 结构 (8 字节/舵机)**:
```c
typedef struct __attribute__((packed)) {
    uint8_t id;       // 舵机 ID (1-18)
    uint8_t acc;      // 加速度 (0-254, 0=最大)
    int16_t pos;      // 目标位置 (0-4095)
    uint16_t time;    // 运动时间 (ms)
    uint16_t speed;   // 目标速度 (0-3000, 0=最大速度)
} ServoCtrlParam_t;
```

**示例** (Python):
```python
# 控制 ID=1 的舵机，位置=2048, 时间=1000ms, 速度=1000, 加速度=50
packet = bytes([
    0xA5, 0x5A,           # 帧头
    0x10,                 # Type: 舵机控制
    0x09,                 # Len: 1(count) + 8(params) = 9
    0x01,                 # Count: 1 个舵机
    0x01, 50,             # ID=1, Acc=50
    0x00, 0x08,           # Pos=2048 (0x0800)
    0xE8, 0x03,           # Time=1000ms (0x03E8)
    0x10, 0x27,           # Speed=1000 (0x2710)
])
checksum = sum(packet) & 0xFF
packet += bytes([checksum])
```

### 3.2 扭矩使能控制 (TYPE_TORQUE_CTRL = 0x11)

**用途**: 批量开启或关闭所有舵机的扭矩输出（即"上锁"或"解锁"）。

**Data 载荷结构**:
```
+--------+
| Enable |
+--------+
  1 byte
```

| 值 | 说明 |
|----|------|
| 0 | 关闭扭矩（舵机进入自由模式，可用于手动拖动） |
| 1 | 开启扭矩（舵机进入位置控制模式） |

**示例** (Python):
```python
# 开启所有舵机扭矩
packet = bytes([0xA5, 0x5A, 0x11, 0x01, 0x01])
checksum = sum(packet) & 0xFF
packet += bytes([checksum])
```

### 3.3 心跳检测 (TYPE_PING = 0x01)

**用途**: 检测下位机是否在线。

**Data 载荷**: 任意（下位机会原样返回）

**示例** (Python):
```python
packet = bytes([0xA5, 0x5A, 0x01, 0x00])
checksum = sum(packet) & 0xFF
packet += bytes([checksum])
```

---

## 4. 反馈数据（下位机 → 上位机）

### 4.1 强化学习全状态包 (TYPE_RL_STATE = 0x40)

**用途**: 50Hz 频率推送 IMU + 所有舵机的反馈数据，用于强化学习控制。

**Data 载荷结构**:
```
+----------+----------+----------+----------+----------+
| IMU(36B) | SrvCount | Srv1(11B)| Srv2(11B)| ...      |
+----------+----------+----------+----------+----------+
```

**IMU 数据结构 (36 字节)**:
```c
typedef struct {
    float acc[3];    // 加速度计 (g), ±16g 量程
    float gyro[3];   // 陀螺仪 (deg/s), ±2000deg/s 量程
    float angle[3];  // 欧拉角 (deg), Roll/Pitch/Yaw
    uint8_t updated; // 更新标志位
} IMU_Data_t;  // 36 bytes (float 占 4 字节)
```

**ServoFBParam 结构 (11 字节/舵机)**:
```c
typedef struct __attribute__((packed)) {
    uint8_t id;        // 舵机 ID
    int16_t pos;       // 当前位置 (0-4095)
    int16_t speed;     // 当前速度
    int16_t load;      // 当前负载
    int16_t current;   // 电流 (暂未实现，固定为 0)
    uint8_t voltage;   // 电压 (暂未实现，固定为 0)
    uint8_t temp;      // 温度 (暂未实现，固定为 0)
} ServoFBParam_t;  // 11 bytes
```

**解析示例** (Python):
```python
import struct

def parse_rl_state(data):
    # data 是去掉帧头、类型、长度、校验和后的纯载荷
    imu_data = data[0:36]
    acc = struct.unpack('<fff', imu_data[0:12])   # 加速度
    gyro = struct.unpack('<fff', imu_data[12:24]) # 陀螺仪
    angle = struct.unpack('<fff', imu_data[24:36])# 欧拉角
    
    servo_count = data[36]
    servos = []
    offset = 37
    for i in range(servo_count):
        s = data[offset:offset+11]
        sid, pos, speed, load, current, voltage, temp = struct.unpack('<bhhh hBB', s)
        servos.append({'id': sid, 'pos': pos, 'speed': speed, 'load': load})
        offset += 11
    
    return {
        'acc': acc,
        'gyro': gyro,
        'angle': angle,
        'servos': servos
    }
```

### 4.2 IMU 单独数据包 (TYPE_SENSOR_IMU = 0x30)

**用途**: 仅推送 IMU 数据（当前固件主要使用 TYPE_RL_STATE 合并包）。

---

## 5. 舵机总线协议（下位机 ↔ ST3215 舵机）

### 5.1 舵机寄存器地址

| 地址 | 寄存器名 | 读写 | 说明 |
|------|----------|------|------|
| 5 | ID | R/W | 舵机 ID (1-254) |
| 6 | 波特率 | R/W | 波特率设置 |
| 9-10 | 最小角度限制 | R/W | 限位 |
| 11-12 | 最大角度限制 | R/W | 限位 |
| 40 | 扭矩使能 | R/W | 0=关闭，1=开启 |
| 41 | 加速度 | R/W | 0-254 |
| 42-43 | 目标位置 | R/W | 0-4095 |
| 44-45 | 运动时间 | R/W | 0-65535 ms |
| 46-47 | 目标速度 | R/W | 0-3000 |
| 55 | 锁定标志 | R/W | 1=锁定参数 |
| 56-57 | 当前位置 | R | 反馈 |

### 5.2 同步写指令 (Sync Write)

下位机使用 `0x83` 同步写指令，一次性发送多个舵机的控制参数，确保所有舵机同时动作。

**指令格式**:
```
0xFF 0xFF 0xFE Length Instruction StartAddr DataLen ID1 Data1 ... IDn Datan Checksum
```

---

## 6. 4 路电机驱动板协议（UART5）

### 6.1 物理层
- **接口**: UART5 (TX: PB12, RX: PB2)
- **波特率**: 115200
- **协议**: 文本行协议，`$` 开头，`#` 结尾

### 6.2 控制指令（下位机 → 驱动板）

| 指令 | 说明 | 示例 |
|------|------|------|
| `$pwm:v1,v2,v3,v4#` | 设置 4 路电机 PWM (-3600~3600) | `$pwm:1000,-500,0,2000#` |
| `$stop#` | 停止所有电机 | `$stop#` |
| `$mtype:3#` | 设置电机类型 | `$mtype:3#` |
| `$mline:546#` | 设置编码器线数 | `$mline:546#` |
| `$wdiameter:75#` | 设置轮径 (mm) | `$wdiameter:75#` |
| `$deadzone:100#` | 设置死区补偿 | `$deadzone:100#` |
| `$upload:0,0,1#` | 设置上传数据格式 | `$upload:0,0,1#` |

### 6.3 反馈数据（驱动板 → 下位机）

**速度反馈格式**: `$MSPD:v1,v2,v3,v4#`
- 单位：**RPS (转/秒)** × 10
- 解析时需乘以 10 转换为 RPM

**示例**: `$MSPD:2.05,2.10,2.00,2.15#` 表示 4 个电机转速分别为 20.5, 21.0, 20.0, 21.5 RPM

---

## 7. 开发指南

### 7.1 上位机连接流程

```python
import serial
import struct
import time

# 1. 打开串口
ser = serial.Serial('COM3', 115200, timeout=0.1)

# 2. 发送 PING 检测连接
def send_ping():
    packet = bytes([0xA5, 0x5A, 0x01, 0x00])
    packet += bytes([sum(packet) & 0xFF])
    ser.write(packet)

# 3. 发送舵机控制命令
def send_servo_cmd(servo_params):
    """
    servo_params: list of dict, e.g.
    [{'id': 1, 'pos': 2048, 'speed': 1000, 'time': 1000, 'acc': 50}]
    """
    data = bytes([len(servo_params)])
    for s in servo_params:
        data += struct.pack('<BBhHH',
            s['id'], s['acc'], s['pos'], s['time'], s['speed'])
    
    packet = bytes([0xA5, 0x5A, 0x10, len(data)]) + data
    packet += bytes([sum(packet) & 0xFF])
    ser.write(packet)

# 4. 接收反馈数据
def read_feedback():
    # 等待帧头
    while ser.read() != b'\xA5':
        pass
    if ser.read() != b'\x5A':
        return None
    
    pkt_type = ser.read(1)[0]
    pkt_len = ser.read(1)[0]
    data = ser.read(pkt_len)
    checksum = ser.read(1)[0]
    
    # 验证校验和
    expected = (0xA5 + 0x5A + pkt_type + pkt_len + sum(data)) & 0xFF
    if checksum != expected:
        return None
    
    return {'type': pkt_type, 'data': data}
```

### 7.2 常见问题

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| 舵机无响应 | 扭矩未开启 | 发送 TYPE_TORQUE_CTRL 开启扭矩 |
| 反馈数据丢失 | USB 带宽不足 | 降低 FeedbackTask 发送频率 |
| 舵机不同步 | 使用了单独写指令 | 确保使用 SyncWrite 指令 |
| 电机速度反馈为 0 | 驱动板未配置 | 检查 `$upload` 指令 |

---

## 8. 附录

### 8.1 文件结构
```
stm32_code/
├── Core/
│   ├── Inc/
│   │   ├── usb_cmd_handler.h    # USB 协议定义
│   │   ├── st_servo.h           # 舵机驱动接口
│   │   ├── Motor.h              # 电机控制接口
│   │   └── imu_handler.h        # IMU 驱动接口
│   └── Src/
│       ├── freertos.c           # FreeRTOS 任务实现
│       ├── usb_cmd_handler.c    # USB 接收处理
│       ├── st_servo.c           # 舵机驱动实现
│       ├── Motor.c              # 电机 PID 控制
│       └── imu_handler.c        # IMU 数据解析
├── GEMINI.md                    # 本文档
└── 电机和舵机说明文档/
    ├── 舵机协议手册.pdf
    └── 4 路电机驱动板控制指令.pdf
```

### 8.2 版本历史
| 版本 | 日期 | 变更说明 |
|------|------|----------|
| V1.0 | 2024-XX-XX | 初始版本，支持 6 舵机 + IMU |
| V2.0 | 2026-03-31 | 扩展至 18 舵机，增加 4 路电机控制 |
